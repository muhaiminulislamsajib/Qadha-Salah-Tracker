<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Overlay Alert Activity for Salat alerts over other apps & lock screen -->
        <activity
            android:name=".PrayerAlertDialogActivity"
            android:exported="false"
            android:theme="@style/Theme.PrayerDialog"
            android:showWhenLocked="true"
            android:turnScreenOn="true"
            android:excludeFromRecents="true"
            android:launchMode="singleInstance" />

        <!-- Broadcast Receiver for System Boot -->
        <receiver
            android:name=".UserUnlockReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Alarm Broadcaster Receiver for Daily Prayers custom timings -->
        <receiver
            android:name=".PrayerAlarmReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.ACTION_PRAYER_ALARM" />
                <action android:name="com.example.ACTION_PRAYER_REMINDER" />
            </intent-filter>
        </receiver>

        <!-- Direct Notification Action Receiver (YES, NO, REMIND 15M) -->
        <receiver
            android:name=".PrayerActionReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.example.ACTION_RECORD_YES" />
                <action android:name="com.example.ACTION_RECORD_NO" />
                <action android:name="com.example.ACTION_RECORD_REMIND_15M" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
